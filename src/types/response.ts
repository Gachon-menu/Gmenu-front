import { DayMenusProps, TodayMenusProps } from "./props";

export interface TodayMenusResponse {
  todayMenus: TodayMenusProps[];
}

export interface DayMenusResponse {
  dayMenus: DayMenusProps[];
}
